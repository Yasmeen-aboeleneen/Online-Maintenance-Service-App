import 'package:flutter/material.dart';

class LoginWithNum extends StatelessWidget {
  const LoginWithNum({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}